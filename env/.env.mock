# ========================================
# 开发环境 - Apifox Mock 模式
# ========================================
# 使用命令: npm run dev:mock
# 描述: 使用 Apifox 云端 Mock 服务，无需启动后端
# ========================================

NODE_ENV=development

# Mock 模式控制
VITE_USE_MOCK=apifox

# 临时演示模式控制
# true: 展示临时演示活动（骨干大团建活动）
# false: 仅展示真实活动数据
VITE_DEMO_MODE=true

# ========================================
# Apifox Mock 服务配置
# ========================================
# 注意：每个模块对应一个独立的 Mock Base URL
# 获取方式：Apifox → 项目设置 → Mock 设置 → 云端 Mock 前置 URL

# 认证模块 Mock URL (登录模块)
# 路径前缀: /api/auth/*
VITE_AUTH_MOCK_URL=https://m1.apifoxmock.com/m1/7269221-6996856-6378684

# 活动模块 Mock URL (活动管理模块)
# 路径前缀: /api/events/* (不包括 enrollments 子路径)
# 包含：活动CRUD、活动列表等接口
VITE_EVENT_MOCK_URL=https://m1.apifoxmock.com/m1/7269221-6996856-6383074

# 文件上传模块 Mock URL
# 路径前缀: /api/file/*
# 包含：文件上传接口
VITE_FILE_MOCK_URL=https://m1.apifoxmock.com/m1/7269221-6996856-default

# 报名管理模块 Mock URL (报名管理 + 通知)
# 路径前缀: /api/events/*/enrollments/*
# 包含：报名信息、批量导入、发送通知等接口
VITE_ENROLLMENT_MOCK_URL=https://m1.apifoxmock.com/m1/7269221-6996856-6423067

# 匹配模块 Mock URL (关键词提取)
# 路径前缀: /api/matching/*
VITE_MATCHING_MOCK_URL=https://m1.apifoxmock.com/m1/7269221-6996856-default

# 匹配结果发布模块 Mock URL
# 路径前缀: /api/match-groups/*
# 包含：获取匹配结果、发布匹配结果等接口
VITE_MATCH_RESULT_MOCK_URL=https://m1.apifoxmock.com/m1/7269221-6996856-6423586

# 词嵌入模块 Mock URL
# 路径前缀: /api/embeddings/*
VITE_EMBEDDING_MOCK_URL=https://m1.apifoxmock.com/m1/7269221-6996856-6408049

# 规则设置模块 Mock URL (Phase 2 & 3)
# 路径前缀: /api/match-rules/* 和 /api/match/:eventId/generate 和 /api/match/:eventId/execute
# 包含：AI 生成规则、批量保存规则、约束条件配置、匹配执行等接口
# 注意：/api/match/:eventId/generate 和 /api/match/:eventId/execute 接口都在这个 Mock 环境中
VITE_RULES_MOCK_URL=https://m1.apifoxmock.com/m1/7269221-6996856-6423882

# Apifox Mock Token（所有模块共用）
VITE_APIFOX_TOKEN=7WpWR1HdKZIChw9BM0LQ3

# 默认 Base URL（兼容性配置，Mock 模式下不使用）
VITE_API_BASE_URL=http://localhost:3001
